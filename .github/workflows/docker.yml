name: Docker

on:
  push:
    branches: [main]
    tags: ['v*']
    paths:
      - '.github/workflows/docker.yml'
      - 'docker/**'
      - 'apps/**'
      - 'cmake/**'
      - 'config/**'
      - 'include/**'
      - 'src/**'
      - 'tools/**'
      - 'CMakeLists.txt'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/docker.yml'
      - 'docker/**'
      - 'apps/**'
      - 'cmake/**'
      - 'config/**'
      - 'include/**'
      - 'src/**'
      - 'tools/**'
      - 'CMakeLists.txt'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Normalize container image name
        id: image
        shell: bash
        run: echo "name=${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.image.outputs.name }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
            type=raw,value=slamforge-ci,enable=${{ github.event_name == 'pull_request' }}

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: docker/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          load: ${{ github.event_name == 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test CLI in image
        if: github.event_name == 'pull_request'
        run: docker run --rm ${{ env.REGISTRY }}/${{ steps.image.outputs.name }}:slamforge-ci --help
